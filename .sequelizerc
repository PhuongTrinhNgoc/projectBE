const path = require('path');

module.exports = {
  'config': path.resolve(__dirname, 'src', 'config', 'config.js'), // Thay đổi đường dẫn đến config.js
  'models-path': path.resolve(__dirname, 'src', 'db', 'models'),   // Thay đổi đường dẫn đến models
  'migrations-path': path.resolve(__dirname, 'src', 'db', 'migrations'), // Thay đổi đường dẫn đến migrations
  'seeders-path': path.resolve(__dirname, 'src', 'db', 'seeders')   // Thay đổi đường dẫn đến seeders
};
